/**
 * RoleLayout — thin wrapper that renders DashboardLayout.
 * App.tsx uses <RoleLayout /> as the shared layout for all protected routes.
 */
import DashboardLayout from './DashboardLayout';

export default DashboardLayout;
